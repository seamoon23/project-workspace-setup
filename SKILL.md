---
name: workspace-setup
description: 프로젝트 저장소에 dev/docs/works 3분할 폴더 구조, workspace.md 사이트맵, AGENTS.md(CLAUDE.md 참조), .gitignore 규칙을 셋팅한다. 모드 - (없음) 전체 셋팅 / dry-run 계획만 / audit 규칙 점검 / update 사이트맵 동기화. "폴더 구조 셋팅", "워크스페이스 초기화", "저장소 정리" 요청 시 사용.
---

# workspace-setup — 저장소 3분할 구조 셋팅

프로젝트 저장소를 **"소스는 소스대로, 문서는 문서대로, 1회성 작업물은 따로"** 나눠 정리하는 스킬.
한 번 셋팅해두면 "이 파일 어디에 두지?"를 매번 고민하지 않아도 되고, git에는 코드와 저술 원본만 남는다.
템플릿은 이 스킬 폴더의 `templates/` 하위에 있다.

## 사용법 (모드)

| 호출 | 동작 | 파일 변경 |
| --- | --- | --- |
| `/workspace-setup` | 전체 셋팅. **계획표를 먼저 보여주고, 승인받은 뒤에만 실행** | 승인 후에만 |
| `/workspace-setup dry-run` | 전체 셋팅과 같은 분석을 하되 계획표 출력까지만 | 없음 |
| `/workspace-setup audit` | 이미 셋팅된 저장소가 규칙대로 유지되고 있는지 점검 | 없음 (보고만) |
| `/workspace-setup update` | workspace.md 사이트맵을 실제 폴더 상태와 동기화 | workspace.md만 |

처음이면 `dry-run`부터 돌려보는 것을 권한다. 무엇이 어떻게 바뀔지 변경 없이 전부 볼 수 있다.

## 3분할 원칙 (분류 기준)

| 폴더 | 성격 | git | 판단 기준 |
| --- | --- | --- | --- |
| `dev/` | 로컬 전용 | 제외 | 내 PC 환경에 묶인 것 (배포 스크립트, 세션 메모, 폐기 코드 보관) |
| `docs/` | 문서·양식 | **추적(기본)** | 계속 참조·갱신되는 저술 원본. 소실되면 복구 불가한 것 |
| `works/` | 요청작업 | 제외 | 특정 시점 1회성. 요청 원본 + 산출물. `YYYYMMDD_작업제목/{요청원본,report}/` |

**헷갈릴 때**: "다음 달에도 이 파일을 열어볼까?" → 그렇다 `docs/` · 아니다 `works/`

## 안전 원칙 (모든 모드 공통 — 절차보다 우선한다)

1. **2단계 실행**: 파일을 만들거나 옮기기 전에 반드시 전체 계획표(생성 파일 목록 + 이동 계획표)를 보여주고 승인을 받는다. 승인 전에는 어떤 파일도 변경하지 않는다.
2. **삭제 금지**: 이 스킬은 어떤 파일도 삭제하지 않는다. 이동(Move)과 추가(append/병합)만 한다. 비우고 남은 빈 폴더 제거만 예외.
3. **덮어쓰기 금지**: AGENTS.md·workspace.md·.gitignore·CLAUDE.md 모두 기존 파일이 있으면 읽고 병합안을 제시한다. 새로 쓰는 경우는 해당 파일이 없을 때뿐이다.
4. **시작 전 git 상태 확인**: 커밋 안 된 변경이 있으면 먼저 커밋(또는 stash)을 권한다. 깨끗한 상태에서 시작해야 문제가 생겨도 `git status` 하나로 전부 되돌릴 수 있다.
5. **검증 가능한 완료 보고**: 이동 내역 전체(원위치 → 새 위치)를 남기고, 이동 전후 파일 개수를 대조해 유실이 없음을 확인한다.

---

## 절차: 전체 셋팅

### 0. 현황 파악 (변경 없음)
- git 저장소 여부 확인. 아니면 `git init` 여부를 사용자에게 확인.
- `git status`로 작업 트리가 깨끗한지 확인. 아니면 커밋/stash 권유 (안전 원칙 4).
- `CLAUDE.md` 존재 확인. 없으면 `/init` 실행을 권하거나 최소본을 먼저 만든다.
- **기존 `AGENTS.md`·`workspace.md`·`dev/`·`docs/`·`works/` 존재 확인.** 이미 있으면 내용을 읽고 병합안을 준비한다 (안전 원칙 3). 특히 `dev/`가 빌드 소스로 쓰이는 프로젝트면 git 제외 대상에서 빼고 다른 이름(예: `local/`)을 합의한다.
- 기존 문서·폴더를 전부 나열해 3분할 기준으로 분류한다.

### 1. 계획표 제시 → 승인 (변경 없음)
아래를 한 번에 보여주고 승인을 받는다. `dry-run` 모드는 여기서 끝.
- 생성할 파일·폴더 목록
- 이동 계획표: `현재 위치 → 이동 위치` (전체 파일)
- **git 추적 중인 파일을 제외 폴더(dev/·works/)로 옮기는 항목은 ⚠ 표시** — 커밋 시 저장소 이력에서 빠지므로 사용자가 인지하고 선택해야 한다.
- docs/ git 정책 질문: **A. 추적(권장)** — 저술 원본 소실 방지, 대용량 1회성은 works/로 · **B. 제외** — API 덤프·리서치로 용량이 커질 프로젝트. B면 `.gitignore`에 `/docs/` 추가, workspace.md의 git 열도 맞춰 수정.

### 2. 폴더 생성
```
dev/scripts/          docs/guide/  docs/api/  docs/db/  docs/design/
works/_backlog/
```
프로젝트 성격에 안 맞는 하위폴더(예: DB 없는 프로젝트의 docs/db)는 뺀다. 빈 폴더는 git이 추적하지 않으므로 docs/ 하위는 실제 문서를 넣을 때 만들어도 된다.

### 3. .gitignore
`templates/gitignore-append.txt` 내용을 기존 `.gitignore` 끝에 append (중복 확인 후).

### 4. AGENTS.md
`templates/AGENTS.md`를 루트에 복사하고:
- `{PROJECT_NAME}` 치환
- "핵심 규칙 요약" 절에 해당 프로젝트 CLAUDE.md의 절대 규칙을 3~6줄로 요약해 채운다.

### 5. workspace.md
`templates/workspace.md`를 루트에 복사하고 실제 생성/이동된 폴더·파일을 반영해 채운다. 없는 절은 지운다. **추측으로 채우지 말고 실제 존재하는 것만 적는다.**

### 6. CLAUDE.md 병합
`templates/claude-section.md`의 "폴더 구조" 절을 CLAUDE.md에 추가한다 (이미 유사 절이 있으면 갱신).

### 7. 검증·보고
- `git status`: `dev/`·`works/`가 목록에 안 뜨고, AGENTS.md·workspace.md는 추적 대상으로 잡히는지 확인.
- 이동 전후 파일 개수 대조 (안전 원칙 5).
- 완료 보고: 생성 파일 목록 + 이동 내역 전체 + 검증 결과.

---

## 절차: audit 모드 (점검만, 변경 없음)

셋팅 후 시간이 지나면 규칙이 흐트러진다. 다음을 점검해 **위반 목록 + 권장 조치**로 보고한다. 이동·수정은 사용자가 승인한 뒤에만.

1. `git status`에 dev/·works/ 하위 파일이 노출되는가 (.gitignore 누락·경로 변경)
2. `docs/` 안에 1회성 작업물이 들어와 있는가 (날짜 폴더 `YYYYMMDD*`, "요청"·"회신"·"작성본" 류 파일명)
3. 루트에 떠도는 파일이 있는가 (workspace.md 루트 목록에 없는 신규 파일)
4. `works/` 폴더명이 `YYYYMMDD_작업제목` 규칙을 지키는가, 산출물이 `report/` 하위에 있는가
5. workspace.md 사이트맵과 실제 폴더가 일치하는가 (불일치 시 `update` 모드 권유)

## 절차: update 모드 (workspace.md만 갱신)

실제 폴더 구조를 스캔해 workspace.md를 현행화한다. **파일 이동은 하지 않는다.**
- 사라진 폴더·파일 항목 제거, 새로 생긴 폴더 추가, `works/` 최근 작업 목록 갱신
- "자주 찾는 것" 표는 기존 내용 유지 (사용자가 채운 것)
- 상단 "최종 갱신" 날짜 변경

---

## FAQ

- **이미 굴러가는 프로젝트에 써도 되나?** → 된다. 승인 전에는 아무것도 변경하지 않으므로 `dry-run`으로 먼저 결과를 보면 된다.
- **잘못됐을 때 되돌리려면?** → 완료 보고의 이동 내역을 역순으로 옮기면 된다. git 추적 파일은 `git checkout`/`git stash pop`으로 복원 가능 (시작 전 깨끗한 상태였다면).
- **CLAUDE.md가 없는 프로젝트라면?** → `/init`으로 먼저 만들고 다시 실행하는 것을 권한다. AGENTS.md가 CLAUDE.md를 참조하는 구조이기 때문.

## 운영 규칙 (셋팅 후 workspace.md에 포함됨)

1. 반출본(export 사본)을 저장소에 중복 보관하지 않는다 — 원본에서 재생성.
2. works/ 산출물은 재생성 가능한 상태로 둔다 (생성 절차를 md로 함께 남김).
3. 폐기 코드는 지우지 말고 `dev/archive/`로 이동.
